[API_KEYS]
# Hunter.io：邮箱前缀选 auto 时使用。在 https://hunter.io/api-keys 获取
hunter = xxxxx-xxxx-xxx

[ACCOUNT]
# LinkedIn 登录账户（密码仅本地保存，脚本实际用下方 [SESSION] Cookie 登录）
email = lxxxx@gmail.com
password =
name = Chuihui Lee
public_id = chuihui-lee-8b8789a0
profile_url = https://www.linkedin.com/in/xx

[CREDS]
# 与 [ACCOUNT] 保持一致，兼容旧版字段名
linkedin_username = leechuihui2025@gmail.com
linkedin_password =

[DEFAULTS]
# 配合 -y/--yes 或省略命令行参数时使用
email_domain =
email_prefix = auto
filter_company = n
company_id =
open_html = y
# Max profiles to save with -y (0 = all, up to LinkedIn 1000). Override: --max-results N
max_results = 0
# Max API pages to fetch with -y (40 profiles/page). Override: --max-pages N
max_pages = 0

[SESSION]
# LinkedIn 会话（须含 li_at + JSESSIONID）。过期后执行：
#   python3.12 scripts/sync_linkedin_cookie.py
cookie = 
li_at = 
